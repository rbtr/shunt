apiVersion: v1
kind: Service
metadata:
  name: shunt
  labels:
    app.kubernetes.io/name: shunt
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shunt
  ports:
    - name: health
      port: 8080
      targetPort: health
